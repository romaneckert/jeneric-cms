extends ../layout-signed-out

block append head
    title=trans('jeneric.user.password.title')
    meta(name="description" content=trans('jeneric.user.password.description'))

block content

    h1.h2=trans('jeneric.user.password.headline')

    .jeneric-text
        p=trans('jeneric.user.password.text')

    +error(errors=form.errors)

    +form(method='post' form=form)
        .form-group
            label(for='password')=trans('jeneric.form.label.password')
            +textfield.form-control(id='password' type='password' name='password' form=form required='required')

        .form-group
            label(for='password-repeat')=trans('jeneric.form.label.password_repeat')
            +textfield.form-control(id='password-repeat' type='password' name='passwordRepeat' form=form required='required')

        button.btn.btn-primary(type='submit')=trans('jeneric.form.submit')
