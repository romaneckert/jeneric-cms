extends ../../../layout/jeneric/signed-out

block append head
    title=view.translate('jeneric.user.password_reset.title')
    meta(name='description' content=view.translate('jeneric.user.password_reset.description'))

block content

    h1.h2=view.translate('jeneric.user.password_reset.headline')

    .jeneric-text
        p=view.translate('jeneric.user.password_reset.text')

    +error(errors=form.errors)

    +form(method='post' form=form)
        .form-group
            label(for='email')=view.translate('jeneric.form.label.email')
            +textfield.form-control(id='email' type='email' name='email' form=form required='required')

        button.btn.btn-primary(type='submit')=view.translate('jeneric.form.submit')



