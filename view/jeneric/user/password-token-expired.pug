extends ../layout-signed-out

block append head
    title=view.translate('jeneric.user.password_token_expired.title')
    meta(name="description" content=view.translate('jeneric.user.password_token_expired.description'))

block content

    h1.h2=view.translate('jeneric.user.password_token_expired.headline')

    .jeneric-text
        p=view.translate('jeneric.user.password_token_expired.text')
